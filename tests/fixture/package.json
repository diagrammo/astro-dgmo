{
  "name": "astro-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "if command -v claim-port >/dev/null 2>&1; then claim-port 4341 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; astro dev --port 4341",
    "preview": "if command -v claim-port >/dev/null 2>&1; then claim-port 4341 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; astro preview --port 4341"
  },
  "dependencies": {
    "@diagrammo/dgmo": "0.61.0",
    "astro": "^6.4.8",
    "astro-dgmo": "link:../..",
    "remark-dgmo": "0.14.2"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1 <1",
      "sharp": ">=0.35.0 <1"
    }
  },
  "engines": {
    "node": ">=20.6"
  }
}
