{
  "name": "astro-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "if command -v claim-port >/dev/null 2>&1; then claim-port 4341 || exit 1; fi; astro dev --port 4341",
    "preview": "if command -v claim-port >/dev/null 2>&1; then claim-port 4341 || exit 1; fi; astro preview --port 4341"
  },
  "dependencies": {
    "@diagrammo/dgmo": "0.60.0",
    "astro": "^6.0.0",
    "astro-dgmo": "link:../..",
    "remark-dgmo": "0.14.0"
  },
  "engines": {
    "node": ">=20.6"
  }
}
