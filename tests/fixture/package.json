{
  "name": "astro-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "if command -v claim-port >/dev/null 2>&1; then claim-port 4341 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; ASTRO_DEV_BACKGROUND=1 astro dev --port 4341",
    "preview": "if command -v claim-port >/dev/null 2>&1; then claim-port 4341 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; ASTRO_PREVIEW_BACKGROUND=1 astro preview --port 4341"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.2",
    "@diagrammo/dgmo": "0.66.0",
    "astro": "^7.2.0",
    "astro-dgmo": "link:../..",
    "remark-dgmo": "0.14.3"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1 <1",
      "sharp": ">=0.35.0 <1"
    }
  },
  "engines": {
    "node": ">=20.6"
  }
}
